editPosts() {
    this.data.forEach((element) => {
    const e= Object.element.replace('cf_', '');      
    e = this.data.default_value;
    console.log(e)
  },

  editPosts() {
    var result = this.posts.map(o => Object.fromEntries(Object.entries(o).map(([k,]) => [k.replace('cf_', ''),])));
  console.log(result);
  },

  this.fields.forEach((element) => {
    element.default_value = this.;
    });

    editPosts(data) {
      var yyy= this.data.default_value;
      for(let i in yyy){
        console.log(yyy[i])
      };
      yyy.forEach( async (element) => {
        yyy[element] = Object.values(data);
      });
    },
  },